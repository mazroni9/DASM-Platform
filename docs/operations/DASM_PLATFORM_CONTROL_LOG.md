# DASM Platform — Control Log

مرجع تشغيلي دائم للمشروع. يُحدّث مع كل دفعة عمل رئيسية.

هذا الملف هو المرجع التشغيلي الأعلى للمشروع، ويُعتمد عند تعارض الذاكرة أو التقارير المختصرة أو الحالة المتخيلة مع الواقع الحالي في المستودع والإنتاج.

---

## 1. الغرض من الملف

- أن يكون **مرجع العمل الأعلى** (source of truth) للنشاط التشغيلي للمنصة
- توثيق القواعد والسياسات المعتمدة
- تتبع baseline الحالة الحالية والسجل الزمني للدفعات
- تحديد المهام النشطة والمؤجلة والأولويات

---

## 2. القواعد التشغيلية الثابتة

| القاعدة | الوصف |
|---------|-------|
| **Branch → PR → merge** | لا يتم الدفع مباشرة إلى `master`. كل تعديل عبر فرع ثم PR ثم مراجعة ودمج. |
| **Minimal Safe Production Fix** | التصحيحات الإنتاجية تكون بالحد الأدنى: إصلاح ما هو mock أو broken فقط، دون توسع غير ضروري. |
| **فصل نطاقات العمل** | إصلاحات Dashboard منفصلة عن إصلاحات البنية (auth/rewrite/env). كل منهما في PR مستقل. |
| **لا أرقام وهمية** | الإحصائيات والعروض تستخدم بيانات حقيقية فقط. إن تعذر — إخفاء العنصر أو خصم unavailable. |
| **لا روابط مكسورة** | أي quick action أو رابط يؤدي إلى صفحة غير مكتملة يُخفى أو يُزال حتى يُنفّذ. |
| **لا تكرار غير ضروري** | إزالة التكرار في وصول المستخدم (مثل ازدواج زر الإعدادات/الملف الشخصي). |
| **مراجعة Control Log إلزامية قبل أي عمل** | لا يُبدأ أي عمل جديد في المشروع (بما في ذلك عبر أدوات المساعدة) قبل مراجعة `docs/operations/DASM_PLATFORM_CONTROL_LOG.md`. الملف هو **المرجع التشغيلي الأعلى**. عند تعارض أي ذاكرة أو توجيه مختصر أو افتراض سابق مع اللوق، تكون **الأولوية دائمًا للّوق**. أي مهمة جديدة تُسجَّل أو تُراجع مقابل اللوق قبل التنفيذ. |

---

## 2.5 سياسة نسبة العمل في الـ PRs والـ Commits (PR / Commit Attribution Policy)

هذا القسم جزء من الانضباط التشغيلي للمستودع، ويهدف إلى إبقاء السجل نظيفًا ومتسقًا.

| القاعدة | الوصف |
|---------|-------|
| **لا إشارة إلى أدوات في السجل** | لا نضع أي إشارة إلى Cursor أو أي أداة مساعدة داخل commit messages أو PR descriptions أو merge descriptions. |
| **حذف النصوص المولدة تلقائيًا** | يجب حذف أي نص مضاف تلقائيًا مثل `Made with Cursor` أو ما شابهه **قبل** تنفيذ الدمج. |
| **صيغة نسبة العمل عند الحاجة** | عند الحاجة الفعلية إلى attribution، الصيغة المعتمدة: *Planned, reviewed, and approved by mazroni9*. |
| **الأفضلية للمحايدية** | إذا لم تكن هناك حاجة فعلية إلى attribution، يُفضّل ترك العنوان والوصف مهنيين ومحايدين دون أي نسبة إلى أداة. |

---

## 2.6 سياسة تحسينات UX/UI البصرية فقط (الفرع الطويل العمر الرسمي)

| القاعدة | الوصف |
|---------|-------|
| **الفرع الرسمي** | `program/ux-improvements-2` هو الفرع طويل العمر **الوحيد** المعتمد لتحسينات **الواجهات البصرية فقط**: التنسيقات، البطاقات، المسافات، ترتيب العناصر، polish بصري للجوال، واجهة الصفحة الرئيسية، الداشبورد، مجلس السوق، وأي تحسين **مظهري** لا يغيّر عقود API أو قاعدة البيانات. |
| **ممنوع على هذا الفرع** | أي من: تغييرات **backend**، **قاعدة بيانات**، **seeders**، **migrations**، **API**، **auth**، **runtime / providers**، **إصلاحات crash جوال مرتبطة بـ Firebase أو منطق تشغيل**، أو أي إصلاح **تقني** غير بصري. تلك الأنواع تذهب إلى **فرع قصير العمر مستقل** ثم **PR مستقل**. |
| **انضباط الدفعات** | كل دفعة بصرية قابلة للمراجعة والاختبار؛ لا دمج تغييرات تقنية ضمن برنامج UX البصري. |

### 2.6.1 تصحيح تنظيمي (الفرع السابق `program/ux-improvements`)

- استُخدم الفرع `program/ux-improvements` بشكل **مختلط** ودخلت فيه إصلاحات **تقنية** (مثل Firebase / runtime) لا تنتمي إلى **UX البصري الصرف**.
- تم اعتماد `program/ux-improvements-2` **لتصحيح هذا الخلط** وفرض نطاق بصري صريح.
- **من الآن فصاعدًا** يُمنع خلط إصلاحات backend / database / runtime مع برنامج تحسينات الواجهة البصرية على الفرع الطويل العمر.

### 2.6.2 حالة الفرع القديم `program/ux-improvements` والحذف

- **PR #204** مدمج في `master` (يشمل إصلاح تصفح الجوال المتعلق بـ Firebase Messaging وتحسينات بصرية للصفحة الرئيسية ضمن دمج واحد).
- دُمج جزء من التعديلات المرتبطة بمجلس السوق لاحقًا في `master` عبر **PR #207** (تخفيف بداية الصفحة) و**PR #208** (تصنيف في الـ seeder). يُراجع عند الحاجة ما إذا بقي على الفرع `program/ux-improvements` في الأصل commits غير مدمجة أو مكررة مع ما دُمج، ثم يُقرّر دمج المتبقي أو حذف الفرع بعد التحقق.
- **لا يُحذف** الفرع `program/ux-improvements` من GitHub حتى التحقق من عدم بقاء عمل معتمد عليه فقط على ذلك الفرع (انظر أعلى).
- بعد التحقق وعدم الحاجة للفرع، يُحذف من الأصل لإغلاق المسار المختلط.

---

## 3. Baseline الحالة الحالية

| المكون | الحالة |
|--------|--------|
| **Frontend** | Next.js على Vercel (Production + Preview) |
| **Backend** | Laravel على Render (`dasm-platform-backend`) |
| **Dashboard stats** | حقيقية عبر `GET /api/dashboard/stats` |
| **My Sales** | حقيقية عبر `GET /api/settlements?seller=me` |
| **API rewrites** | Production: `API_BACKEND_URL` = `https://api.dasm.com.sa`؛ قابلة للتكوين |
| **/ping** | Route Handler محلي يعيد 200 |
| **التسجيل (Frontend)** | يمر عبر عميل API موحّد (shared axios) وطلبات same-origin إلى `/api/register` — لا مسار تسجيل منفصل بعميل مختلف عن بقية الواجهة. |
| **المستخدم العادي (`user`)** | لا ينتظر تفعيلًا إداريًا بعد التسجيل ضمن السياسة الحالية؛ ما يزال يحتاج توثيق البريد قبل الدخول حسب التدفق المعتمد. |
| **أنواع أخرى (مثل dealer / venue_owner / investor)** | تبقى ضمن مسار المراجعة أو التفعيل الإداري حسب النوع والسياسة. |
| **مجموعة الموافقات التشغيلية (Operational approval group)** | مفهوم منفصل عن Spatie teams/roles: جدول `approval_group_members` (عضوية فريدة لكل مستخدم + أعلام قدرات) و`approval_requests` (طابور عام). إدارة العضوية لـ `super_admin` فقط (`/admin` → مجموعة الموافقات). الطابور في `/admin/approval-requests` و`/admin/control-room/approval-requests`. بعد توثيق البريد لـ dealer / venue_owner / investor يُنشأ طلب `business_account` ويُرسل بريد + إشعار قاعدة بيانات + FCM لكل عضو نشط في المجموعة (دون opt-out). طلبات مجلس السوق: `POST /api/council-studio/access-request` مع حزم writer/editor/publisher/moderator. صفحة `/admin/market-council/permissions` تبقى أداة يدوية للصلاحيات. |
| **verify-email وإعادة إرسال التحقق** | تعتمد على عميل API موحّد وطلبات same-origin إلى مسارات `/api` — لا اعتماد على استدعاء مباشر من المتصفح بعنوان backend منفصل من صفحة التحقق. |
| **حذف المستخدمين (لوحة الإدارة)** | محصور على `super_admin` في الواجهة والخادم؛ ممنوع حذف `super_admin` أو حذف الحساب الحالي للمُدير. |
| **دور `programmer`** | مضاف في النظام ومسارات الإدارة؛ مزامنة Spatie تُضبط عبر migrations + provisioner مخصص — **لا** يُشترط تشغيل `php artisan db:seed --class=RolesAndPermissionsSeeder` كخطوة نشر خاصة بهذه الحزمة. |
| **صلاحيات council.\*** | مُعرّفة في `RolesAndPermissionsSeeder`؛ `DatabaseSeeder` يستدعي هذا الـ seeder عند الزرع الكامل. الصفحة `/admin/market-council/permissions` تعمل بعد زرع الصلاحيات. حالة empty state السابقة كانت ناتجة عن غياب seed في البيئة وليست نقصًا في التعريف بالكود. |
| **برنامج UX البصري** | الفرع الطويل العمر الرسمي: `program/ux-improvements-2` (بصري فقط؛ انظر القسم 2.6). الفرع `program/ux-improvements` **موقوف عن الاستخدام** بعد التصحيح التنظيمي؛ لا يُحذف من الأصل حتى دمج المتبقيات (انظر 2.6.2). |

---

## 4. السجل الزمني المختصر للدفعات الأخيرة

| التاريخ التقريبي | الدفعة | النتيجة |
|-------------------|--------|---------|
| 2026-03 | **PR #211** — verify-email / إعادة التحقق عبر عميل API موحّد؛ دور `programmer`؛ حذف المستخدمين لـ `super_admin` فقط؛ تحسينات محدودة لإدارة المستخدمين | دمج في master: مسارات التحقق عبر same-origin؛ `programmer` عبر migrations + provisioner دون اشتراط `db:seed` لهذه الحزمة؛ DELETE محصور صلاحيًا |
| 2026-03 | **PR #210** — موافقة تلقائية للمستخدم العادي؛ تنظيف pending والقوائم من `type = user` | دمج في master: فصل تفعيل المستخدم العادي عن قائمة انتظار الإدارة؛ أعدادات متسقة |
| 2026-03 | **PR #209** — إصلاح التسجيل: shared axios و same-origin `/api/register` | دمج في master: مسار تسجيل موحّد |
| 2026-03 | **PR #208** — تصنيف مجلس السوق «ثقافة التاجر المحترف» عبر seeder | دمج في master: بيانات تصنيف في الـ seeder |
| 2026-03 | **PR #207** — إزالة hero header من صفحة مجلس السوق | دمج في master: تخفيف بداية الصفحة |
| 2026-03 | **PR #206** — AI pricing widget في car details و dashboard | دمج في master: ويدجت تسعير استرشادي بالذكاء الاصطناعي |
| 2026-03 | **PR #204** — تصفح الجوال (Firebase Messaging) + تحسينات بصرية للصفحة الرئيسية | دمج في master: حماية FCM على Safari/unsupported؛ تحسين سطح الـ hero في الوضع الفاتح؛ إزالة عنوان «اختر سوقك المتخصص» المكرر؛ ضبط مسافات قسم الأسواق |
| 2026-03 | **PR #205** — إعادة تنظيم استراتيجية الفروع (`program/ux-improvements-2`، اللوق، قواعد Cursor) | PR إلى master: سياسة الفرع البصري، مراجعة اللوق إلزامية، توثيق الفرع القديم والمتبقيات |
| 2026-03 | **PR #199** — إصلاح infinite loading في صفحة صلاحيات مجلس السوق | دمج: فصل loadingPermissions/loadingUsers، إنهاء التحميل دائمًا، حالات خطأ وempty واضحة |
| 2026-03 | **PR #198** — قواعد .cursor/rules | دمج: تحديث أو إضافة قواعد للمشروع |
| 2026-03 | **PR #197** — تحديث Control Log | دمج: توثيق الحالة التشغيلية |
| 2026-03 | **PR #196** — Mobile dashboard priority actions | دمج: إجراءات سريعة فوق المحتوى على الجوال، ترتيب عملي |
| 2026-03 | **PR #195** — Market Council permissions management | دمج: صفحة إدارة صلاحيات مجلس السوق في لوحة المسؤول |
| 2026-03 | **PR #194** — Dashboard visual unification | دمج: توحيد ألوان البطاقات، شد بطاقة الترحيب |
| 2026-03 | **PR #193** — Mobile client crash fix | دمج: تقوية ConnectionQuality و Navbar ضد استثناءات iPhone/Safari |
| 2026-03 | **PR #192** — Profile + header polish | دمج: تحسين contrast في UserMenu، تهذيب profile |
| 2026-03 | **PR #191** — Header / Dashboard UX | دمج: تحسينات UX للهيدر والـ dashboard |
| 2026-03 | **PR #190** — Login redirect fallback | دمج: fallback لتسجيل الدخول عند تأخر التحويل |
| 2026-03 | **PR #189** — Council Studio access fix | دمج: إصلاح ظهور استوديو مجلس السوق |
| 2026-03 | **PR #188** — Coming Soon (shipping + technical inspection) | دمج: صفحات Coming Soon |
| 2026-03 | **PR #187** — سياسة Attribution | دمج: توثيق سياسة نسبة العمل |
| 2026-03 | **PR #186** — إضافة Control Log | دمج: إنشاء هذا الملف |
| 2026-03 | **PR #185** — إصلاح API rewrite | دمج: إعادة توجيه `/api/*` إلى backend مُكوّن |
| 2026-03 | **PR #184** — إصلاح Dashboard للإنتاج | دمج: stats حقيقية، My Sales API، إزالة quick actions مكسورة، إزالة ازدواج الإعدادات |

---

## 5. المنجزات المؤرشفة

- [x] Dashboard stats حقيقية + trends
- [x] My Sales API
- [x] إزالة quick actions المكسورة
- [x] إزالة زر الإعدادات المكرر
- [x] إصلاح rewrite: دعم `API_BACKEND_URL` و `NEXT_PUBLIC_API_URL`
- [x] shipping و technical inspection كـ Coming Soon
- [x] إصلاح ظهور Council Studio
- [x] login redirect fallback
- [x] تحسينات Header / Dashboard UX
- [x] تحسين contrast في UserMenu وتهذيب profile
- [x] توحيد ألوان dashboard وشد بطاقة الترحيب
- [x] إصلاح mobile client crash في iPhone/Safari
- [x] إضافة صفحة وصلاحيات Market Council في لوحة المسؤول
- [x] إصلاح spinner اللانهائي في صفحة صلاحيات مجلس السوق
- [x] إثبات أن صلاحيات council.* وأدوار مجلس السوق مُعرّفة في RolesAndPermissionsSeeder
- [x] استعادة عمل صفحة صلاحيات مجلس السوق بعد تشغيل seed
- [x] إعادة ترتيب Dashboard على الجوال: الإجراءات الأساسية أولًا
- [x] إصلاح شهادة www.dasm.com.sa (توثيق وتوجيه يدوي)
- [x] تثبيت الباكند الإنتاجي عبر api.dasm.com.sa
- [x] اعتماد `API_BACKEND_URL = https://api.dasm.com.sa` في Production وعدم الاعتماد على `NEXT_PUBLIC_API_URL` في الإنتاج
- [x] دمج PR #204: حماية Firebase Messaging على الجوال/Safari وتحسينات بصرية محدودة للصفحة الرئيسية
- [x] توحيد مسار التسجيل عبر shared axios client و same-origin `/api/register` (PR #209)
- [x] فصل تفعيل المستخدم العادي عن انتظار تفعيل إداري في القوائم والعدادات (PR #210)
- [x] إصلاح verify-email وإعادة إرسال التحقق عبر عميل API موحّد (PR #211)
- [x] إضافة دور `programmer` ومساراته وإدارة Spatie دون اشتراط `db:seed` لهذه الحزمة (PR #211)
- [x] قصر حذف المستخدمين في لوحة الإدارة على `super_admin` (PR #211)
- [x] تحسين شارات الأدوار والفلترة وحالة التفعيل/التوثيق في صفحة إدارة المستخدمين ضمن النطاق (PR #211)
- [x] إضافة تصنيف حقيقي لمجلس السوق «ثقافة التاجر المحترف» عبر seeder (PR #208)
- [x] إزالة hero header الثقيل من صفحة مجلس السوق (PR #207)
- [x] إضافة AI pricing widget إلى car details و dashboard (PR #206)

---

## 6. المهام النشطة الحالية

- **Smoke test بعد دفعات auth/admin (#209–#211):** تسجيل جديد، توثيق بريد، صفحة verify-email، إعادة إرسال التحقق، والتأكد من عدم ظهور أخطاء اتصال أو CORS في المسارات المعتمدة
- **التحقق من استقرار تعيين دور `programmer` من لوحة الإدارة** ومزامنة Spatie بعد تغيير النوع
- **التحقق من أن حذف المستخدمين لا يظهر ولا يعمل إلا لـ `super_admin`** وأن القيود على الخادم تبقى سارية
- **مراقبة استقرار `/admin/users`** بعد تعديلات الأدوار والحذف والفلاتر
- مراقبة استقرار Production وربط API على `api.dasm.com.sa` وإعدادات Vercel env
- مراجعة نهائية لتجربة الجوال حيث ينطبق (PR #196 وما بعده)
- استخدام صفحة «صلاحيات مجلس السوق» لتعيين الصلاحيات بدل الفحص اليدوي حيث يلزم
- مراجعة ودمج PR #201 إن وُجد اعتماد اتجاه «الحسابات المميزة فقط»؛ الصفحة أداة للحسابات المميزة/الاستثناءات وليست بديلًا كاملًا لإدارة المستخدمين
- **برنامج تحسينات الواجهة البصرية** عبر الفرع الطويل العمر `program/ux-improvements-2` فقط (انظر القسم 2.6)
- **متابعة الفرع القديم `program/ux-improvements`:** التحقق من التداخل مع ما دُمج عبر PRs #207 و#208 ثم حسم المتبقي أو حذف الفرع من الأصل (انظر 2.6.2)
- **واجهة اقتراحات وآراء المستخدمين:** مكان مستقل في واجهة المنصة لاستقبال اقتراحات المستخدمين وآرائهم وتقييماتهم للمنصة، مع إرسالها إلى لوحة الكنترول روم. يُحلل يدويًا/شخصيًا وكذلك عبر الذكاء الاصطناعي، مع فرز المقترحات وأخذ الصالح منها. يُفضّل أن يكون مستقلاً عن مجلس السوق، ووُضِع في جزء غير حرج من الصفحة الرئيسية (مثل: «آراء زوار ورواد DASM-e»)
- **برمجة الفحص عبر الورش محليًا** مع تهيئته لاحقًا ليصبح تطبيقًا على iPhone وAndroid؛ يُبنى داخليًا ثم يُربط لاحقًا عبر API
- **تتبع واسترجاع تاريخ «مجلد الذكاء الصناعي»** الذي كان موجودًا في المشروع ثم حُذف؛ تحديد عبر أي PR دخل أولًا وعبر أي PR خرج أو حُذف لاحقًا؛ اعتماد ذلك كمسار فحص لاحق مهم
- **متابعة ملف إصلاح بعض المشاكل التقنية الحالية**
- **إكمال مشروع الاختبارات الحالي وإغلاقه** وفق ملف الاعتماد/التسليم المطلوب في مشروع منصة مستقل

---

## 7. المهام المؤجلة

| المهمة | السبب | الأولوية |
|--------|-------|----------|
| shipping integration | Coming Soon وُضِع؛ التكامل لاحقًا | متوسطة |
| technical inspection integration | Coming Soon وُضِع؛ التكامل لاحقًا | منخفضة |
| wallet withdraw API | خارج نطاق العمل الحالي | متوسطة |
| تحسينات إضافية غير حرجة على الجوال | polish لاحق بعد استقرار الإنتاج | منخفضة |
| تحسينات slug/loading لمجلس السوق المؤجلة سابقًا | غير حرجة | منخفضة |

---

## 8. المخاطر المعروفة

| المخاطر | التخفيف |
|---------|---------|
| احتمال تضارب envs بين Production و Preview | مراجعة Vercel Environment Variables وضبط قيم مختلفة إن لزم |
| الاعتماد المشترك على rewrites و envs | أي تغيير في `API_BACKEND_URL` أو rewrites يحتاج deploy |
| بقاء بعض PRs المدموجة القديمة بوصف فيه attribution غير مرغوب | لا يؤثر تشغيليًا؛ عدم تكراره مستقبلًا |
| خطورة التعديلات الإنتاجية السريعة من الجوال بدون توثيق | توثيق أي تعديل حرج قبل التنفيذ |
| ظهور empty state في صفحة صلاحيات المجلس | غالبًا بسبب عدم تنفيذ seed في البيئة. الإجراء: `php artisan db:seed --class="Database\Seeders\RolesAndPermissionsSeeder"` ثم `php artisan permission:cache-reset` |
| تراكم تحسينات UX داخل فرع طويل العمر بدون انضباط | إبقاء كل دفعة منفصلة وقابلة للمراجعة والاختبار |
| استمرار عطل متصفح الجوال في الإنتاج | تقليلًا بعد PR #204؛ متابعة تحقق إنتاجي؛ أي انحدار جديد يُعالج في **فرع تقني قصير العمر** لا في `program/ux-improvements-2` |
| دفعات UX غير قابلة للمراجعة | ضرورة إبقاء دفعات UX قابلة للمراجعة والاختبار |
| اختلاف أو تأخر بين `users.type` ودور Spatie عند تغيير النوع من لوحة الإدارة | اختبار تعيين الأنواع ذات المزامنة؛ مراجعة السجلات عند أول تعيين `programmer` في بيئة جديدة |
| ميزة `programmer` تعتمد على اكتمال migrations (بما فيها ضمان دور Spatie) في البيئة | تشغيل `php artisan migrate` بعد الدمج؛ عدم الاعتماد على `db:seed` للحزمة نفسها |
| الحاجة إلى تحقق إنتاجي بعد تغييرات verify-email و admin users | إجراء smoke test موثّق؛ أي خلل يُفتح في تذكرة/PR تقني مستقل |

---

## 9. ترتيب الأولويات

1. **مراقبة الاستقرار** بعد دفعات auth/admin (#209–#211): smoke tests وإثبات سلوك التسجيل والتحقق وحذف المستخدمين و`programmer`
2. **أي إصلاح تقني لاحق** يُثبت فقط عبر فرع قصير العمر وPR مستقل — لا إعادة فتح نفس النقاش دون خلل مؤكد
3. تثبيت الاستقرار العام (Production، API، envs)
4. برنامج UX البصري على فرعه المخصص (`program/ux-improvements-2`)
5. حسم اتجاه صفحة صلاحيات المجلس حيث ينطبق
6. واجهة اقتراحات وآراء المستخدمين
7. الفحص عبر الورش
8. تتبع مجلد الذكاء الصناعي
9. إكمال مشروع الاختبارات

---

## 10. طريقة تحديث هذا الملف

- تُحدَّث الأقسام عند كل دفعة عمل رئيسية
- تُنقَل المهام المكتملة إلى "المنجزات المؤرشفة"
- تُحدَّث "السجل الزمني" بإضافة صف جديد لكل PR مدمج
- تُعدَّل "المهام النشطة" و"المؤجلة" حسب التقدم
- تُراجَع "الخطوة التالية المعتمدة" قبل بدء أي عمل جديد
- لا يُضاف أي سر أو قيمة بيئة حساسة

---

## 11. الخطوة التالية المعتمدة

- **قبل أي عمل جديد:** مراجعة هذا الملف (`docs/operations/DASM_PLATFORM_CONTROL_LOG.md`) كمرجع أعلى
- **بعد دفعات auth/admin الأخيرة (#209–#211):** تخطيط وتنفيذ smoke tests في الإنتاج/المعاينة حسب التوفر للتأكد من سلامة التسجيل والتحقق ولوحة المستخدمين
- **أي إصلاح تقني لاحق:** فرع قصير العمر وPR مستقل؛ لا دمج مع برنامج UX البصري إلا عند ارتباط مباشر موثّق
- **تحسينات بصرية فقط:** الفرع `program/ux-improvements-2` + PRs منفصلة قابلة للمراجعة
- **الفرع القديم `program/ux-improvements`:** التحقق من المتبقي بعد دمج #207 و#208 ثم حسم الدمج أو الحذف من الأصل (انظر 2.6.2)
- بقية الأعمال حسب ترتيب الأولويات (القسم 9)

---

## 12. ملاحظة إدارية

بعض PRs الأخيرة اندمجت مع attribution غير مرغوب داخل وصف الـ PR. ذلك لا يؤثر تشغيليًا على المنصة، ولكنه يخالف سياسة السجل (القسم 2.5) ويجب عدم تكراره مستقبلًا.

PR #200 أُغلق لأنه أصبح redundant بعد دمج أصل إصلاح infinite loading في PR #199.

تم اعتماد سياسة الفرع `program/ux-improvements-2` وقاعدة «مراجعة اللوق أولًا» وتوثيق حالة الفرع القديم في القسم 2.6 وما يليه.

---

*آخر تحديث: 2026-03 — إضافة baseline لمجموعة الموافقات التشغيلية والطابور (فرع `feature/approval-group-and-request-alerts`؛ رقم PR يُحدَّث عند الدمج)*
