# Configuration for Cloudflare Pages
name = "dasm-platform"
compatibility_date = "2023-10-30"

[build]
command = "pnpm run build"

# This is the key property Cloudflare is looking for
[site]
bucket = ".next"

# The key field that was missing - specifies where the build output is located
pages_build_output_dir = ".next"