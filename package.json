{
  "name": "mazad-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd Frontend-local && npm run dev\" \"cd backend && npm run dev\"",
    "dev:frontend": "cd Frontend-local && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "cd Frontend-local && npm run build",
    "start": "cd Frontend-local && npm run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
