{
  "name": "dasm-may2025",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@8.7.1",
  "scripts": {
    "dev": "concurrently \"cd Frontend-local && pnpm run dev\" \"cd backend && pnpm run dev\"",
    "dev:frontend": "cd Frontend-local && pnpm run dev",
    "dev:backend": "cd backend && pnpm run dev",
    "build": "cd Frontend-local && pnpm run build",
    "start": "cd Frontend-local && pnpm run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "lucide-react": "^0.507.0",
    "node-fetch": "^3.3.2"
  }
}
