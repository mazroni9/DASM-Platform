# ✅ اختبارات المزادات — تم تنفيذها (Completed)

اختبارات تم تنفيذها ولها نتيجة (Pass أو Fail). العيوب مسجلة تحت **رقم العلة**.

---

## 📋 جدول الاختبارات المنفذة

| ID | الوصف | السيناريو | الخطوات | النتيجة المتوقعة | النتيجة الفعلية | الحالة | رقم العلة |
|---|------|-----------|---------|-----------------|----------------|--------|-----------|
| **TC001** | إنشاء سيارة جديدة من Exhibitor | Exhibitor ينشئ سيارة جديدة | 1. تسجيل الدخول كـ Exhibitor<br>2. الذهاب إلى `/exhibitor/add-car`<br>3. ملء البيانات المطلوبة<br>4. رفع 5 صور<br>5. حفظ | • السيارة تُنشأ بنجاح<br>• `auction_status = 'available'`<br>• الصور تُرفع<br>• إشعار للمدراء | **🔴 مشاكل حرجة:**<br>1. زر "إضافة السيارة" لا يعمل - `return` مبكر في دالة التحقق يمنع الإرسال<br>2. الصور (images_list) لا تصل للباك إند - يتم إرسال JSON فقط بدون `multipart/form-data`<br>3. الانتقال بين الخطوات بدون التحقق من البيانات - يمكن الانتقال ببيانات ناقصة<br>4. رسائل التحقق غير كاملة - بعض الرسائل غير واضحة أو مكررة | ❌ Fail | BUG-001, BUG-002, BUG-003, BUG-004 |
| **TC001B** | إنشاء سيارة جديدة - صفحة `/add/Car` | User ينشئ سيارة من `/add/Car` | 1. تسجيل الدخول<br>2. الذهاب إلى `/add/Car`<br>3. ملء البيانات المطلوبة<br>4. رفع صور السيارة، تقارير الفحص، بطاقة التسجيل، وكافة الملفات المطلوبة (ملفات ثقيلة >5MB)<br>5. حفظ | • السيارة تُنشأ بنجاح<br>• جميع الملفات (الصور، التقارير، بطاقة التسجيل) تُرفع<br>• لا timeout<br>• في حالة الفشل: rollback كامل | **🔴 مشاكل حرجة:**<br>1. **Timeout عند رفع ملفات ثقيلة** - يعطي `timeout of 30000ms exceeded` عند رفع صور السيارة، تقارير الفحص، بطاقة التسجيل، أو أي ملفات ثقيلة (أكبر من 5MB تقريباً)<br>2. **Transaction Issue** - بعد timeout، عند إعادة المحاولة يقول "VIN موجود" - يعني الـ request نفذ جزئياً (السيارة حُفظت لكن الملفات لم تُرفع) - المشكلة: `DB::transaction()` موجود لكن timeout يحدث قبل `DB::commit()`<br><br>**الحلول المقترحة:**<br>1. فصل upload files في endpoint منفصل (upload أولاً ثم إنشاء السيارة)<br>2. تحسين/ضغط الملفات (الصور، التقارير، إلخ) قبل الرفع<br>3. Validation على حجم الملف (max 5MB مثلاً لكل ملف)<br>4. زيادة timeout للـ request<br>5. ضمان rollback كامل عند timeout (استخدام try-catch أفضل مع `DB::rollBack()`) | ❌ Fail | BUG-011, BUG-012 |
| **TC002** | موافقة Admin على السيارة | Admin يراجع ويفضل السيارة | 1. تسجيل الدخول كـ Admin<br>2. الذهاب إلى `/admin/cars`<br>3. البحث عن السيارة<br>4. فتح التفاصيل<br>5. الموافقة | • `auction` يُنشأ تلقائياً<br>• `auction.status = 'scheduled'`<br>• `car.auction_status = 'in_auction'`<br>• `end_time` محسوب | **🔴 مشاكل حرجة:**<br>1. زر "معالجة" لا يعمل - Route `/api/admin/cars/{id}/process-auction` غير موجود (404)<br>2. عدم تطابق حالة المزاد - الفرونت يقرأ `car.auction_status` فقط وليس `auction.status`<br>3. منطق الموافقة - يعمل فقط إذا `car.auction_status === 'available'`، يتخطى الحالات الأخرى<br>4. منطق الرفض - `car.auction_status` يتحدث لكن المزاد يبقى موجوداً بدون تحديث | ❌ Fail | BUG-005, BUG-006, BUG-007, BUG-008 |
| **TC003** | موافقة Control Room على المزاد | Admin يوافق من غرفة التحكم | 1. تسجيل الدخول كـ Admin<br>2. الذهاب إلى `/admin/control-room`<br>3. اختيار المزاد<br>4. تعيين `opening_price = 75000`<br>5. تفعيل `approved_for_live = true`<br>6. الموافقة | • `control_room_approved = true`<br>• `opening_price = 75000`<br>• إذا 16:00-19:00 → `status = 'live'`<br>• `car.auction_status = 'in_auction'` | **🔴 مشاكل حرجة:**<br>1. الصفحة تعلق على "جاري التحميل" - Route `GET /api/admin/cars/pending` غير موجود (404)<br>2. لا يوجد معالجة أخطاء - الصفحة تبقى في حالة loading بدون رسالة خطأ | ❌ Fail | BUG-009, BUG-010 |
| **TC003B** | صفحة الحراج المباشر - عدم وجود جلسات | User يدخل صفحة `/auctions/auctions-1main/live-market` بدون جلسات نشطة | 1. تسجيل الدخول كـ User<br>2. الذهاب إلى `/auctions/auctions-1main/live-market`<br>3. عدم وجود live sessions حالياً | • رسالة لطيفة: "لا توجد جلسات حالياً، انتظر"<br>• تصميم UI مناسب<br>• لا تظهر أخطاء تقنية | **🔴 مشاكل حرجة:**<br>1. **Error Message غير لطيف** - يظهر error "No live session found" بشكل تقني وغير مناسب للمستخدم النهائي<br>2. **UI غير مناسب** - يجب عرض رسالة ودية مع تصميم مناسب بدلاً من error message | ❌ Fail | BUG-013 |
| **TC003C** | صفحة الحراج المباشر - عرض بيانات السيارة | User يفتح صفحة المزاد المباشر | 1. تسجيل الدخول كـ User<br>2. الذهاب إلى `/auctions/auctions-1main/live-market`<br>3. وجود مزاد نشط<br>4. فحص عرض بيانات السيارة | • جميع حقول السيارة تُعرض بشكل صحيح<br>• البيانات واضحة ومنظمة<br>• لا توجد حقول مفقودة | **🔴 مشاكل حرجة:**<br>1. **عرض حقول السيارة غير صحيح** - حقول السيارة لا تُعرض بشكل صحيح أو منظم في صفحة المزاد المباشر | ❌ Fail | BUG-014 |
| **TC004** | مزايدة صحيحة | User يزايد بمبلغ صحيح | 1. تسجيل الدخول كـ User 1<br>2. الذهاب إلى `/auctions/auctions-1main/live-market`<br>3. إدخال مبلغ أعلى من `opening_price`<br>4. تأكيد | • المزايدة تُقبل<br>• `current_bid` يتحدث<br>• `last_bid_time` = الآن<br>• الرصيد يُخصم<br>• تحديث Realtime بدون refresh | **🔴 مشاكل حرجة:**<br>1. **Refresh كامل للصفحة** - بعد المزايدة الناجحة، يتم عمل refresh كامل للصفحة بدلاً من تحديث realtime<br>2. **لا تحديث Realtime** - السعر لا يتحدث فوراً للمستخدمين الآخرين بدون refresh | ❌ Fail | BUG-015, BUG-016 |
| **TC004B** | مزايدة بمبلغ أقل من opening_price | User يزايد بمبلغ أقل من opening_price | 1. تسجيل الدخول كـ User<br>2. الذهاب إلى `/auctions/auctions-1main/live-market`<br>3. `opening_price = 75000`<br>4. محاولة المزايدة بمبلغ أقل (مثلاً 70000) | • المزايدة تُرفض<br>• رسالة خطأ واضحة<br>• `current_bid` لا يتغير<br>• الرصيد لا يُخصم | **✅ السلوك صحيح** - المزايدة تُرفض بشكل صحيح عند محاولة المزايدة بمبلغ أقل من `opening_price` | ✅ Pass | - |
| **TC004C** | Real-time Updates - متابعة المزاد من مستخدمين متعددين | User آخر يتابع المزاد بعد مزايدة | 1. User 1 يزايد بمبلغ صحيح<br>2. User 2 يدخل نفس صفحة المزاد بعد المزايدة<br>3. فحص ظهور آخر سعر فوراً | • User 2 يرى آخر سعر فوراً بدون refresh<br>• التحديث realtime عبر Pusher/WebSocket<br>• لا حاجة لـ refresh يدوي | **🔴 مشاكل حرجة:**<br>1. **لا تحديث Realtime** - عند دخول User 2 بعد مزايدة User 1، لا يظهر آخر سعر فوراً<br>2. **يحتاج refresh** - يجب عمل refresh يدوي لرؤية آخر سعر | ❌ Fail | BUG-017 |

---

**ملف To Do (أثناء الـ refactor / التنفيذ):** [Manual_Test_Cases_Auctions_TODO.md](./Manual_Test_Cases_Auctions_TODO.md)

**آخر تحديث:** 23 يناير 2026  · **الإصدار:** 1.0.0
