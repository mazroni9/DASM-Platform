# نظام المراقبة والتحليل - DASM Platform

## 📋 نظرة عامة

تم تطوير نظام مراقبة وتحليل شامل لمنصة DASM يوفر:
- ✅ متابعة أداء الفريق والمبرمجين
- ✅ مراقبة صحة المنصة والأداء
- ✅ إدارة سجلات الأخطاء والتنبيهات
- ✅ لوحة تحكم احترافية مع رسوم بيانية تفاعلية

---

## 🏗️ البنية المعمارية

### Backend (Laravel)

#### Models (11 نموذج)
```
app/Models/Monitoring/
├── DeveloperMetric.php                 # مقاييس أداء المبرمجين
├── DailyDeveloperSnapshot.php          # لقطات يومية للأداء
├── PlatformHealthMetric.php            # مقاييس صحة المنصة
├── PlatformErrorLog.php                # سجلات الأخطاء
├── PagePerformanceLog.php              # أداء الصفحات
├── DeploymentLog.php                   # سجلات النشر
├── ApiPerformanceLog.php               # أداء APIs
├── DatabasePerformanceLog.php          # أداء قاعدة البيانات
├── FeatureUsageLog.php                 # استخدام الميزات
├── BackupLog.php                       # سجلات النسخ الاحتياطية
├── SystemAlert.php                     # تنبيهات النظام
└── MonitoringConfig.php                # إعدادات المراقبة
```

#### Controllers (3 متحكمات)
```
app/Http/Controllers/Monitoring/
├── MonitoringController.php            # لوحة التحكم الرئيسية (8 endpoints)
├── DeveloperMetricsController.php      # متابعة الفريق (9 endpoints)
└── PlatformHealthController.php        # صحة المنصة (8 endpoints)
```

#### Database Tables (12 جدول)
```sql
-- مقاييس المبرمجين
developer_metrics
daily_developer_snapshots

-- صحة المنصة
platform_health_metrics
platform_error_logs
page_performance_logs
deployment_logs
api_performance_logs
database_performance_logs

-- الاستخدام والتنبيهات
feature_usage_logs
backup_logs
system_alerts
monitoring_configs
```

### Frontend (Next.js + React)

#### Pages (4 صفحات)
```
app/admin/monitoring/
├── page.tsx                            # لوحة المراقبة الرئيسية
├── developers/page.tsx                 # أداء الفريق
├── health/page.tsx                     # صحة المنصة
└── errors/page.tsx                     # سجلات الأخطاء
```

---

## 🔌 API Endpoints

### 1. لوحة المراقبة الرئيسية
```
POST   /api/monitoring/dashboard
       Response: { summary, recent_errors, top_developers, error_trends, performance_metrics }
```

### 2. إدارة الأخطاء
```
GET    /api/monitoring/error-logs?search=&severity=&is_resolved=&page=
POST   /api/monitoring/error-logs/{id}/resolve
```

### 3. متابعة المبرمجين
```
GET    /api/monitoring/developers
GET    /api/monitoring/developers/top-performers
GET    /api/monitoring/developers/needs-attention
GET    /api/monitoring/developers/team-stats
POST   /api/monitoring/developers/compare
GET    /api/monitoring/developers/{id}
PUT    /api/monitoring/developers/{id}
GET    /api/monitoring/developers/{id}/snapshots
POST   /api/monitoring/developers/{id}/snapshots
```

### 4. صحة المنصة
```
GET    /api/monitoring/health/current
GET    /api/monitoring/health/history?hours=24
GET    /api/monitoring/health/summary
POST   /api/monitoring/health/check
GET    /api/monitoring/health/api-performance
GET    /api/monitoring/health/database-performance
GET    /api/monitoring/health/page-performance
GET    /api/monitoring/health/feature-usage
```

---

## 📊 مقاييس الأداء

### مقاييس المبرمجين
- **Productivity Score**: نسبة الإنتاجية (0-100%)
- **Code Quality Score**: جودة الكود (0-100%)
- **Code Review Score**: درجة مراجعة الكود (0-100%)
- **Overall Score**: الدرجة الكلية (0-100%)
- **Total Commits**: إجمالي الـ Commits
- **Total Pull Requests**: إجمالي الـ PRs
- **Tasks Completed**: المهام المنجزة

### مقاييس صحة المنصة
- **CPU Usage**: استخدام المعالج (%)
- **Memory Usage**: استخدام الذاكرة (%)
- **Disk Usage**: استخدام القرص (%)
- **Uptime**: معدل التشغيل (%)
- **Response Time**: وقت الاستجابة (ms)
- **Active Users**: المستخدمون النشطون
- **API Requests/Min**: طلبات API في الدقيقة

### مقاييس الأخطاء
- **Severity**: الخطورة (Critical, High, Medium, Low)
- **Error Type**: نوع الخطأ
- **Endpoint**: المسار المتأثر
- **Resolution Status**: حالة الحل

---

## 🔐 التحكم في الصلاحيات

### Super Admin (المالك)
- ✅ الوصول الكامل لجميع لوحات المراقبة
- ✅ عرض بيانات جميع المبرمجين
- ✅ إدارة التنبيهات والإعدادات
- ✅ تصدير التقارير

### Admin
- ✅ عرض لوحات المراقبة الأساسية
- ✅ عرض بيانات الفريق
- ✅ إدارة الأخطاء

### Moderator
- ⚠️ وصول محدود (حسب الإعدادات)

### Developer
- ❌ لا يمكنهم الوصول إلى لوحات المراقبة

---

## 🎨 واجهة المستخدم

### لوحة المراقبة الرئيسية
```
┌─────────────────────────────────────────────┐
│ لوحة المراقبة                               │
├─────────────────────────────────────────────┤
│ [المبرمجون النشطون] [الأخطاء] [التشغيل] [الاستجابة] │
├─────────────────────────────────────────────┤
│ [استخدام الموارد]  [توزيع الأخطاء]         │
├─────────────────────────────────────────────┤
│ [اتجاهات الأخطاء]  [مقاييس الأداء]         │
├─────────────────────────────────────────────┤
│ [أحدث الأخطاء] [أفضل المبرمجين] [يحتاجون متابعة] │
└─────────────────────────────────────────────┘
```

### صفحة أداء الفريق
- جدول بجميع المبرمجين مع الدرجات
- بطاقات أفضل الأداء
- قائمة يحتاجون متابعة
- رسوم بيانية للمقارنة

### صفحة صحة المنصة
- مؤشرات الموارد الفورية
- رسوم بيانية للأداء
- سجل تاريخي (24 ساعة)
- إحصائيات API وقاعدة البيانات

### صفحة سجلات الأخطاء
- جدول الأخطاء مع البحث والتصفية
- تصنيف حسب الخطورة
- حالة الحل
- ترقيم الصفحات

---

## 📈 مثال على الاستخدام

### 1. عرض لوحة المراقبة الرئيسية
```typescript
// في المتصفح
https://dasm.com.sa/admin/monitoring

// سيعرض:
- ملخص سريع للمقاييس الرئيسية
- أحدث الأخطاء
- أفضل المبرمجين
- رسوم بيانية الأداء
```

### 2. متابعة أداء مبرمج معين
```typescript
// في المتصفح
https://dasm.com.sa/admin/monitoring/developers

// ثم اختر المبرمج لرؤية:
- الدرجات التفصيلية
- السجل التاريخي
- المقارنة مع الآخرين
```

### 3. فحص صحة المنصة
```typescript
// في المتصفح
https://dasm.com.sa/admin/monitoring/health

// سيعرض:
- الحالة الحالية (سليمة/غير سليمة)
- استخدام الموارد
- أداء الـ APIs
- أداء قاعدة البيانات
```

### 4. إدارة الأخطاء
```typescript
// في المتصفح
https://dasm.com.sa/admin/monitoring/errors

// يمكنك:
- البحث عن أخطاء محددة
- التصفية حسب الخطورة
- تحديد الخطأ كمحلول
- عرض التفاصيل الكاملة
```

---

## 🔄 تحديث البيانات

### التحديث الفوري
- لوحة المراقبة: كل 60 ثانية
- صحة المنصة: كل 30 ثانية
- سجلات الأخطاء: عند الحاجة

### جمع البيانات
```php
// في الخلفية (Cron Jobs)
- كل دقيقة: تحديث مقاييس الصحة
- كل 5 دقائق: حساب درجات المبرمجين
- كل ساعة: إنشاء لقطات يومية
- كل يوم: تنظيف البيانات القديمة
```

---

## 🧪 الاختبار

### اختبار الـ Endpoints
```bash
# اختبر لوحة المراقبة
curl -X POST http://localhost:8000/api/monitoring/dashboard \
  -H "Authorization: Bearer {token}"

# اختبر المبرمجين
curl -X GET http://localhost:8000/api/monitoring/developers \
  -H "Authorization: Bearer {token}"

# اختبر الصحة
curl -X GET http://localhost:8000/api/monitoring/health/current \
  -H "Authorization: Bearer {token}"
```

### اختبار الواجهة
1. تسجيل الدخول كـ Super Admin
2. الذهاب إلى `/admin/monitoring`
3. التحقق من ظهور البيانات بشكل صحيح
4. اختبار الرسوم البيانية والتفاعلات
5. اختبار البحث والتصفية

---

## 🚀 الخطوات التالية

### 1. تنفيذ الترحيل
```bash
php artisan migrate
```

### 2. إنشاء بيانات تجريبية
```bash
php artisan tinker
# أنشئ بيانات تجريبية للاختبار
```

### 3. تشغيل Cron Jobs
```bash
# في الإنتاج
* * * * * cd /path/to/project && php artisan schedule:run >> /dev/null 2>&1
```

### 4. تحسين الأداء
- إضافة Caching للبيانات الثقيلة
- تحسين استعلامات قاعدة البيانات
- تقليل حجم الاستجابات

### 5. التوثيق الإضافي
- توثيق كل API endpoint
- أمثلة استخدام متقدمة
- دليل استكشاف الأخطاء

---

## 📞 الدعم والمساعدة

### المشاكل الشائعة

**المشكلة**: لا تظهر البيانات في لوحة المراقبة
**الحل**: 
- تأكد من تنفيذ الترحيل
- تحقق من وجود البيانات في قاعدة البيانات
- تحقق من صلاحيات المستخدم

**المشكلة**: الرسوم البيانية بطيئة
**الحل**:
- أضف Caching
- قلل نطاق البيانات المعروضة
- استخدم Pagination

**المشكلة**: الأخطاء لا تظهر في السجلات
**الحل**:
- تحقق من إعدادات logging
- تأكد من وجود middleware للتسجيل
- تحقق من صلاحيات الملفات

---

## 📝 الملاحظات المهمة

1. **الأمان**: جميع الـ endpoints محمية بـ authentication و authorization
2. **الأداء**: استخدم Caching للبيانات الثقيلة
3. **التوسع**: يمكن إضافة مزيد من المقاييس والرسوم البيانية
4. **التكامل**: يمكن تكامل مع أنظمة تنبيهات خارجية
5. **الأرشفة**: قم بأرشفة البيانات القديمة بشكل دوري

---

## 📄 الملفات المتعلقة

- `/backend/app/Models/Monitoring/` - النماذج
- `/backend/app/Http/Controllers/Monitoring/` - المتحكمات
- `/backend/database/migrations/2025_12_27_000001_create_monitoring_tables.php` - الترحيل
- `/backend/routes/api.php` - الـ Routes
- `/frontend/app/admin/monitoring/` - الصفحات
- `/frontend/app/admin/layout.tsx` - التنقل

---

## ✅ قائمة التحقق

- [x] إنشاء النماذج
- [x] إنشاء المتحكمات
- [x] إنشاء الترحيل
- [x] إضافة الـ Routes
- [x] إنشاء صفحات الواجهة الأمامية
- [x] إضافة التنقل
- [ ] تنفيذ الترحيل (في الإنتاج)
- [ ] إنشاء بيانات تجريبية
- [ ] الاختبار الشامل
- [ ] تحسين الأداء
- [ ] نشر في الإنتاج

---

**آخر تحديث**: 27 ديسمبر 2025
**الإصدار**: 1.0.0
**الحالة**: جاهز للمراجعة والاختبار
